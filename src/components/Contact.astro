<section id="contact">
    <div class="container">
        <div class="contact-panel">
            <p class="section-kicker">Contact</p>
            <h2>Let’s build something dependable.</h2>
            <p class="contact-copy">
                I’m open to software development, quality engineering, and contract
                web opportunities.
            </p>

            <div class="contact-links">
                <a href="mailto:grantponting25@gmail.com" class="contact-link">
                    <span>Email</span><strong>grantponting25@gmail.com</strong>
                </a>
                <a href="https://github.com/grantponting" target="_blank" rel="noopener noreferrer" aria-label="GitHub profile, opens in a new tab" class="contact-link">
                    <span>GitHub</span><strong>@grantponting ↗</strong>
                </a>
                <a href="https://www.linkedin.com/in/grant-ponting/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn profile, opens in a new tab" class="contact-link">
                    <span>LinkedIn</span><strong>Grant Ponting ↗</strong>
                </a>
            </div>
        </div>

        <footer>
            <span>© {new Date().getFullYear()} Grant Ponting</span>
        </footer>
    </div>
</section>

<style>
    #contact { border-bottom: 0; padding-bottom: 2rem; }

    .contact-panel {
        padding: clamp(2rem, 6vw, 4.5rem);
        border: 1px solid var(--border-strong);
        border-radius: var(--radius-lg);
        background: linear-gradient(135deg, rgba(168, 198, 134, 0.07), rgba(113, 143, 104, 0.025));
    }

    .contact-panel h2 { max-width: 17ch; margin-top: 0.65rem; }

    .contact-copy {
        max-width: 39rem;
        margin-top: 1rem;
        color: var(--text-secondary);
        font-size: 1.08rem;
    }

    .contact-links { margin-top: 2.5rem; border-top: 1px solid var(--border-subtle); }

    .contact-link {
        display: grid;
        grid-template-columns: 8rem 1fr;
        gap: 1.5rem;
        padding: 1.15rem 0;
        border-bottom: 1px solid var(--border-subtle);
        color: var(--text-secondary);
    }

    .contact-link span {
        color: var(--text-muted);
        font-family: var(--font-mono);
        font-size: 0.78rem;
        letter-spacing: 0.08em;
        text-transform: uppercase;
    }

    .contact-link strong { color: var(--text-primary); font-weight: 500; }
    .contact-link:hover strong { color: var(--accent-primary); }

    footer {
        display: flex;
        justify-content: center;
        gap: 1rem;
        padding: 2rem 0 0;
        color: var(--text-muted);
        font-size: 0.8rem;
    }

    @media (max-width: 600px) {
        .contact-link { grid-template-columns: 1fr; gap: 0.35rem; }
        .contact-link strong { overflow-wrap: anywhere; }
    }
</style>
